<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8" />
    <title>Hello World</title>
  </head>
  <body>
    <form method="get" action="/chat">
    <h2>User:<%= req.user.username%></h2>
    </section>
      <select name="Chatrooms">
        <% console.log(JSON.stringify(listofrooms, null, '\t')) %>
        <% for(var i=0; i<listofrooms.length; i++) {%>
            <option value="<%= listofrooms[i].id %>"><%= listofrooms[i].roomname %></option>
        <% } %>
      </select>
      <br><br>
      <input type="submit" value="submit" action="/chat">
    </form>
  </body>
</html>
